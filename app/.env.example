# Copy to .env for local development outside Docker Compose.
APP_NAME=zero-trust-demo
VAULT_ADDR=http://localhost:8200

# Stage 2: AppRole credentials (generated by vault-init bootstrap).
# VAULT_ROLE_ID=
# VAULT_SECRET_ID=

# Stage 2: PostgreSQL connection target (credentials come from Vault, not here).
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=appdb
